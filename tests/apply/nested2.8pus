Theorem easyimpl : forall A : Type, forall B : Type, forall C : Type, (A -> B -> C) -> A -> B -> C.
Proof.
    intro a.
    intro b.
    intro c.
    intro f.
    intro x.
    apply f.
    exact x.
Qed.
