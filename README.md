# Pieuvre

Command to launch the interactive UI :

```bash
dune build && rlwrap _build/default/bin/pieuvre.exe
```

Command to run pieuvre on a file :

```bash
dune build && rlwrap _build/default/bin/pieuvre.exe <filename>
```

Command to launch the tests :

```bash
python3 tests/test.py
```

# Work distribution
1. (Maya) Options -reduce -alpha et -typecheck
2. (Maya) Implémentation native des paires, sommes, matchs, ou et et.
3. (Sarah) Types inductifs
4. (Sarah) Lancement sur des fichiers
5. (Sarah & Maya) Suiet de tests
6. (Sarah) Unification